spring:
  application:
    name: horse-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/flyinglions-zsl/horse-config-server
          username: flyinglions-zsl
          password: xxx
          default-lable: master #配置文件分支
          search-paths: config #配置文件所在根目录
